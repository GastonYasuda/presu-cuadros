import React from 'react'

const ListItem = ({ valores }) => {

   // console.log(valores);

    return (
        <div>
            {/* 
            {
                valores.map((listValue, i) => {
                    return (
                        <option value={i} key={i}>{Object.keys(listValue)}</option>

                    )
                })
            } */}
        </div>
    )
}

export default ListItem